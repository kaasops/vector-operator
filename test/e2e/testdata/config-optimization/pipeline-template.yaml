apiVersion: observability.kaasops.io/v1alpha1
kind: VectorPipeline
metadata:
  name: {{INDEX}}
spec:
  sources:
    logs:
      type: kubernetes_logs
  sinks:
    console:
      type: console
      inputs:
        - logs
      encoding:
        codec: json

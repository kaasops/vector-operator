# Same Secret as secret.yaml with a rotated token, used to verify the new value
# reaches the already-running Vector process through the mounted secret-assets
# Secret, without any config change and without a pod rollout.
apiVersion: v1
kind: Secret
metadata:
  name: probe-creds
type: Opaque
stringData:
  token: "runtime-probe-v2"

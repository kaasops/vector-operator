# Backing Secret for the "probe" backend declared by pipeline.yaml. The value is a
# synthetic marker: the probe asserts on it appearing in the agent's stdout, so it is
# deliberately printable and meaningless outside this test.
apiVersion: v1
kind: Secret
metadata:
  name: probe-creds
type: Opaque
stringData:
  token: "runtime-probe-v1"

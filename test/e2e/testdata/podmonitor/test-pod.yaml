apiVersion: v1
kind: Pod
metadata:
  name: test-app
  labels:
    app: test
spec:
  containers:
  - name: nginx
    image: nginx:alpine
    command: ["/bin/sh", "-c"]
    args:
      - |
        while true; do
          echo "Test log message from test-app"
          sleep 5
        done
